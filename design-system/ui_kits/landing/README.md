# UI Kit · Landing Page

Recreation of `Landing v2.dc.html` from the prototype codebase as a static (non-scrollytelling) page: fixed blurred header, hero with tilted product preview, "So funktioniert's" conversation → fact list, working / clarification / offer beats, feature bento, FAQ accordion, closing CTA and footer.

Files: `index.html` (entry), `Landing.jsx` (whole page, composed from DS components).

Not rebuilt: the scroll-driven blob that travels between sections and the sticky beat choreography. Copy is verbatim from the source.
