Profile circle with initials, top-right of every screen.

```jsx
<Avatar initials="HB" interactive onClick={openMenu} />
```

- No photos; initials only. Same outline treatment as IconButton `outline`.
