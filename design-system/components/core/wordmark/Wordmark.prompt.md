Lowercase wordmark for headers and footers; the only brand mark in the product UI.

```jsx
<Wordmark />
<Wordmark size={17} color="var(--rs-ink)" href="#top" />
```

- 500 weight, .04em tracking, always lowercase "roomscout".
- The cube logo (assets/logo-roomscout.png) is only used as a source/portal avatar, never next to the wordmark.
