Top bar for every RoomScout screen.

```jsx
<AppHeader right={<><StatusDot pulse>Scout ist unterwegs</StatusDot><IconButton label="Suche pausieren"><Icon name="pause" size={16} /></IconButton></>} onAvatar={toggleMenu} />
```

- No nav links in the app header; the landing page header is a different pattern.
