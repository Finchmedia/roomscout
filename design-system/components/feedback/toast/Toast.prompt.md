Tells the user something happened in the Scout while they are in settings.

```jsx
<Toast onAction={goToScout} onDismiss={close}>Dein Scout wartet auf deine Freigabe</Toast>
```

- Positioned top-right under the header (right 24, top 96).
