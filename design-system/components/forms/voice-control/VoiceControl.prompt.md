Conversation controls row: Mikro an/aus · Mitschrift · Gespräch beenden.

```jsx
<VoiceControl tone="accent" label="Mikro an"><Icon name="mic" size={26} /></VoiceControl>
<VoiceControl label="Mitschrift"><Icon name="transcript" size={24} /></VoiceControl>
<VoiceControl tone="danger" label="Gespräch beenden"><Icon name="close" size={24} /></VoiceControl>
```

- 76px circles on desktop, 60px narrow; 34px gap.
