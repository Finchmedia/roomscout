Toggle for sources and permissions in settings.

```jsx
<Switch checked={on} onChange={setOn} label="Anbieter kontaktieren" />
```

- Saves immediately in the prototype; show a small "Gespeichert" label next to it.
