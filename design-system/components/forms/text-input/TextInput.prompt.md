Standard input for settings (search, names). For chat use Composer instead.

```jsx
<TextInput value={q} onChange={setQ} placeholder="Quelle oder Region suchen …" />
<TextInput value="0" invalid helper="Bitte eine ganze Zahl größer als 0 eingeben." />
```
