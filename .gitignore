node_modules/
.next/
dist/
.env
.env.*
!.env.example
.DS_Store
*.log
.convex/
.firecrawl/
tsconfig.tsbuildinfo
playwright-report/
test-results/

# keep large media out of the public repo — serve via Vimeo / Convex storage
*.mp4
*.webm
*.mov

# generated eval output (local paths, stack traces)
artifacts/

# design-system renders are large reference images; keep locally, not in git
design-system/assets/renders/

# personal Claude Code overrides
.claude/settings.local.json
